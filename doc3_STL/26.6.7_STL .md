# STL
## 1.vector 容器
```
1.
capacity(); //容器的容量
resize(int num); //重新指定容器的长度为num,若容器变长则以默认值填(0)充新位置,如果容器变短则末尾超出容器长度的元素被删除
resize(int num, elem); //重新指定容器的长度为num,若容器变长则以elem值填充新位置，如果容器变短则末尾超出容器长度的元素被删除
```

```
2.vector 数据存储操作
  at(int idx); //返回索引idx所指的数据如果idx，越界抛出out_of_range异常
  operator[]; //返回索引idx所指的数据如果idx，越界时直接报错
  front(); //返回容器中第一个数据元素
  back(); //返回容器中最后一个元素数据
```

```
3.vector 插入和删除操作
  insert(const_iterator pos, int count, ele); //迭代器指向位置pos,插入count个ele       *要注意迭代器* 如v1.begin
  push_back(ele); //尾部插入元素ele
  pop_back(); //删除最后一个元素
  erase(const_iterator start, const_iterator end); //删除迭代器从start到end之间的元素    --[ )--   
  erase(const_iterator pos); //删除迭代器指向的元素
  clear(); //删除容器中所有元素
```

```
4.**巧用swap收缩内存空间**
  类型+() ----> 匿名对象
  swap(); //你的size有多大我就收缩成多大的容量   如 vector<int>(v1).swap(v1);
  //swap用来交换交换完，匿名对象要释放 释放原本的大容量 保留交换后的小容量
```


```
5.vector 容器提高
  嵌套容器定义一个容器存放v1 v2 v3
```

```c++
  vector<int> v1(5,10);
  vector<int> v2(5,100);
  vector<int> v3(5,1000);
  //需求: 定义一个容器存放v1 v2 v3
  vector< vector<int> > v;
  v.push_back(v1);
  v.push_back(v2);
  v.push_back(v3);

  //遍历要定义他的迭代器
  vector< vector<int> > :: iterator it = v.begin()
  for(;it!=v.end();it++)
  {
     //*it==vector<int>  **对迭代器取*代表的就是尖括号里面的内容vector<int>**
     //容器嵌套最关键的就是 这个*it到底是什么！！！
     vector<int>::iterator mit = (*it).begin();
     for(; mit!=(*it).end(); mit++ )
     {
         cout<<*mit<<"";
     }
         cout<<endl;

  }
```


```c++
//使用STL算法对vector容器进行排序
//排序算法：
sort(v1.begin(),v1.end());

//举例
class Person
{
    friend bool comparePerson(Person ob1, Person ob2);
    friend void PrintVectorPerson(vector<Person> &v);
private:
    int num;
    string name;
    float score;
public:
    Person(){};
    Person(int num, string name, float score)
    {
        this->num = num;
        this->name = name;
        this->score = nscore;

    }
};

bool comparePerson(Person ob1, Person ob2)
{
    return ob1.num < ob2.num;
}

void PrintVectorPerson(vector<Person> &v)   // **访问任何容器不管你是存的什么都要拿他的迭代器**
{
    vector<Person>::iterator it = v.begin();
    for(;it!=v.end();it++)
    {
        //*it==Person
        cout<<(*it).num<<" "<<(*it).name<<" "<<(*it).score<<endl;
        //在这里 num name score 是私有的 要这个Person能访问  需要把这个设置为友元
        //也可以重载输出运算符
    }
}

void test08()
{
    vector <Person> v1;
    v1.push_back(Person(100,"lucky",77.7f));
    v1.push_back(Person(103,"bob",77.7f));
    v1.push_back(Person(101,"tom",77.7f));
    v1.push_back(Person(104,"dema",77.7f));
    v1.push_back(Person(105,"kimi",77.7f));

    printVectorPerson(v1);
    //对定义类型的vector排序，需要指定排序规则   会调用规则
    sort(v1.begin(), v1.end(), comparePerson);
}
```


## 2.deque 容器    ----双向开口的动态数组

```
头插：push_front()
尾插：push_back()
头删：pop_front()
尾删：pop_back()
返回第一个数据：front()
返回最后一个数据：back()
```

## 3.deque 常用api

```
1.deque 构造函数
略
2.deque 赋值操作
略
3.deque 大小操作
略

```

<img width="930" height="475" alt="image" src="https://github.com/user-attachments/assets/cdec1fe0-4e9f-4820-b66f-ea7e1e18c0f2" />



```
4.deque 双端插入和删除操作
  push_back(elem);
  push_front(elem);
  pop_back();
  pop_front();

5.deque 数据存取
  at(idx);
  operator[];
  front();
  back();

6.deque 插入操作
  insert(pos, elem);   //在pos位置插入一个elem元素的拷贝返回新数据的位置
  insert(pos, n, elem);   // 无返回值
  insert(pos, beg, end);  // 无返回值

7.deque 删除操作
  clear();
  erase(beg,end);
  erase(pos);

```

<img width="951" height="565" alt="image" src="https://github.com/user-attachments/assets/0024deab-b104-43a5-8d69-e7cb6076bf18" />




案例
```
有5名选手:选手ABCDE，10个评委分别对每一名选手打分，去除最高分，去除评委中最低分，取平均分。
1.创建五名选手，放到vector中
2.遍历vector 容器，取出来每一个选手，执行for循环，可以把10个评分打分存到deque容器中
3.sort算法对deque容器中分数排序，pop_back pop_front去除最高和最低分
4.deque容器遍历一遍，累加分数，累加分数/d.size()
5.person.score=平均分

#include <iostream>
#include <deque>
#include <algorithm>
using namespace std;

class Person
{
public:
    string name;
    float score;
public:
    Person(){};
    Person(string name, float score)
    {
        this->name = name;
        this->score = score;
    
    }
};

void createPerson(vector<Person> &v)     //创建选手
{
    string tmpName = "ABCDE";
    int i = 0;
    for(i=0;i<5;i++)
    {
        string name = "选手";
        name += tmpName[i];
        v.push_back(Person(name, 0.0f))
    }
    
}

void showPerson(vector<Person> &v)      //展示选手
{
    vector<Person>::iterator it = v.begin();
    for(;it!=v.end();it++)
    {
        //*it == Person
        cout<<(*it).name<<" "<<(*it).score<<endl;
    }
}

void playGame(vector<Person> &v)
{
    //每个人都参加比赛  用迭代器
    vector<Person> :: iterator it = v.begin();
    for(;it!=v.end();it++)
    {
        //定义一个deque容器存放10个评委的分数
        deque<float> d;
        int i = 0;
        for(i=0;i<10;i++)     //10个评委打分
        {
             d.push_back( (float)(rand()%41+60) )   //   (0~40)+60 保证分数在60-100
        }
        //对deque排序
        sort(d.begin(), d.end());
        //去掉最高分和最低分
        d.pop_back();
        d.pop_front();
        //求平局分
        (*it).score = (float)accumulate(d.begin(), d.end(), 0)/d.size(); 

    } 
}
#include<time>

void test02()
{
    vector<Person> v;

    //srand设置随机数种子
    srand(time(NULL));
    //创建5名选手
    createPerson(v);
    //显示选手成绩
    showPerson(v);
    //比赛
    playGame(v)
}

int main(int argc, char* argv[])
{
    test02();
    return 0;
}


```

## 4.stack容器
stack是一种先进后出的数据结构，stack不允许有遍历行为，进栈push,出栈pop

<img width="975" height="489" alt="image" src="https://github.com/user-attachments/assets/eb78006e-6f8b-4f7a-bb72-d4b7e3879072" />

## 5.queue容器
queue是一种先进先出的数据结构他有两个出口，queue容器允许从一端新增元素，从另一端删除元素
```
priority_queue：优先队列 / 堆
priority_queue 可以理解成：每次都能快速取出“最大”或“最小”的元素。
```

## 6.list 链表容器     -----双向链表
```
链表是一种物理存储单元上非连续、非顺序的存储结构，数据元素的逻辑顺序是通过链表中的指针连接次序实现的
链表由一系列结点(链表中每一个元素称之为结点)组成，结点可以在运行时动态生成

结点两部分：
1)存储数据元素的数据域
2)存储下一个结点地址的指针域

list:好处  负责任：
每次插入或删除一个元素就是配置或释放一个元素空间 -----list对于空间的运用绝对精准 不浪费

采用动态存储分配，不会造成内存浪费和溢出，链表执行插入和删除十分方便，修改指针即可，不需要移动大量数据
但是！！!  空间和时间额外消耗大

```
list常用api

```
1.list 构造函数
2.list数据元素插入和删除操作
push_back(elem);
pop_back();
push_front(elem);
pop_front();
insert(pos,elem);  //在pos位置插入elem元素的拷贝返回数据的新位置
insert(pos,n,elem);
insert(pos,beg,end);   //在pos位置插入[beg,end)区间的数据无返回值
clear();
erase(beg,end);
erase(pos);
remove(elem);

3.list 大小操作
4.list 赋值操作
5.list 数据存取

6.list 反转排序
reverse(); //反转列表 例如lst包含1，3，5元素，反转之后lst就包含5，3，1元素
sort();   //list排序
```

<img width="976" height="788" alt="image" src="https://github.com/user-attachments/assets/d8cd63a6-c264-49b5-939e-acfdb224810a" />

### list容器的迭代器是双向迭代器，不支持+2(如果是随机访问迭代器就支持，比如之前的vector容器)，支持++
### STL提供的算法，只支持随机访问迭代器，而list是双向迭代器，所以sort不支持list
```
❌️❌️❌️：sort(l1.begin(),l1.end());
✅️✅️✅️：l1.sort();
```

## 7.set/multiset容器
```
set的特性是：所有元素都会根据元素的键值自动被排序，set的元素既是键值又是实值
set容器的迭代器是只读迭代器，不允许修改键值，会破坏set内部布局

key值(键值)(学号)---------value(实值)(学生信息)
set不允许有两个相同的键值
multiset的特性及其用法和set完全相同唯一的差别在于他允许键值重复

插入元素只能用insert(), 他会根据元素进行排序
```

set容器常用api
```
1.set 构造函数


2.set 赋值操作

3.set 大小操作

4.set 插入和删除操作

5.set 查找操作
find(key); //查找键key是否存在，若存在，返回该键的元素的迭代器，若不存在，返回set.end();
cout(key); //查找键key的元素个数
lower_bound(keyElem); //返回第一个key>=keyElem元素的迭代器
upper_bound(keyElem); //返回第一个key>keyElem元素的迭代器
equal_range(keyElem); //返回容器中key与keyElem相等的上下限的两个迭代器


set 容器内部用的是平衡二叉树去管理通过二叉树去遍历

```

```c++
#include<iostream>
#include<set>

void printSetInt(set<int,MyGreater> &s)
{
    set<int,MyGreater>::iterator it = s.begin()
    for(;it!=s.end();it++)
    {
        cout<<*it<<" ";
    }
    cout<<endl;

}

//仿函数：重载函数调用运算符()的类
class MyGreater
{
public:
    bool operator() (int v1, int v2)
    {
        return v1>v2;
    }
};

void printSetInt(set<int,MyGreater> &s)
{
    set<int,MyGreater>::const_itertator it = s.begin();
    for(;it!=s.end();it++)
    {
        cout<<*it<<" ";
    }
    cout<<endl;
}




void test02()
{
    //set<int,排序规则> s1;   默认情况下的排序是从小到大
    //排序规则必须是类型，你的函数也必须是类型
    set<int,MyGreater> s1;    //传了这个类型MyGreater，内部会用这个类型去实例化一个对象MyGreater()
    s1.insert(30);    
    s1.insert(10);    
    s1.insert(20);    
    s1.insert(50);    
    s1.insert(40);
    printSetInt(s1);
    

}

int main(int argc, char *argv[])
{
    test02();
    return 0;
}



//-------------------------------------------------------------------------------------------------------------


#include<iostream>
#include<set>

//set 存放自定义数据必须修改排序

class MyGreaterPerson;      //提前声明 不然友元那里的friend void printSetPerson(set<Person,MyGreaterPerson> &s); 会识别不到
class Person
{
    friend class MyGreaterPerson;
    friend void printSetPerson(set<Person,MyGreaterPerson> &s);
private:
    int num;
    string name;
    float score'
public:
    Person(){};
    Person(int num, string name, float score)
    {
        this->num = num;
        this->name = name;
        this->score = score;
    }
};

class MyGreaterPerson      //类型的话只能是class了 
{
public:
    bool operator()(Person ob1, Person ob2)
    {
        return ob1.num < ob2.num;
    } 

};

void printSetPerson(set<Person,MyGreaterPerson> &s)
{
    set<Person,MyGreaterPerson>::const_iterator it = s.begin();
    for(;it!=s.end();it++)
    {
        //*it == Person
        cout<<(*it).num<<" "<<(*it).name<<" "<<(*it).score<<" "<<endl;
    }
    
}

void test03()
{
    set<Person,MyGreaterPerson> s;
    s.insert(Person(100, "lucky", 88.8f));
    s.insert(Person(103, "luck", 88.8f));
    s.insert(Person(105, "luc", 88.8f));
    s.insert(Person(102, "lu", 88.8f));
    s.insert(Person(104, "l", 88.8f));
    printSetPerson(s);
}

int main(int argc, char *argv[])
{
    test03();
    return 0;
}

//---------------------------------------------------------------------------------------------------------------
//set 容器的api  ----set 查找操作

#include<iostream>
#include<set>

void printSetInt(set<int> &s)
{
    set<int>::const_itertator it = s.begin();
    for(;it!=s.end();it++)
    {
        cout<<*it<<" ";
    }
    cout<<endl;
}

void test04()
{
    set<int> s1;
    s1.insert(10);
    s1.insert(30);
    s1.insert(50);
    s1.insert(70);
    s1.insert(90);
    printSetInt(s1);

    set<int>::const_iterator ret;
    ret = s1.find(50);
    if(ret!=s1.end())
    {
        cout<<"找到的结果为:"<<*ret<<endl;
    }
    cout<<s1.count(50)<<endl;
}


void test05()
{
    set<int> s1;
    s1.insert(10);
    s1.insert(30);
    s1.insert(50);
    s1.insert(70);
    s1.insert(90);

    //lower_bound(); upper_bound(); 需要迭代器去接
    set<int>::const_iterator ret;
    ret = s1.lower_bound(50);
    if(ret!=s1.end())
    {
        cout<<"下限为"<<*ret<<endl;    //50
    }

    ret = s1.upper_bound(50);
    if(ret!=s1.end())
    {
        cout<<"上限为"<<*ret<<endl;     //70
    }

    //以对组的方式存储上下限 pair

    pair<set<int>::const_iterator,set<int>::const_iterator> pa;
    pa = s1.equal_range(50);
    if(pa.first!=s1.end())
    {
        cout<<"下限为："<<*(pa.first)<<endl;
    }
    if(pa.second!=s1.end())
    {
        cout<<"上限为："<<*(pa.second)<<endl;
    }
    
     
    
}

//multiset:    ---------------------------------------------------------------------------------------
#include<multiset>

void test06()
{
    multiset<int> s1;
    s1.insert(10);
    s1.insert(30);
    s1.insert(10);
    s1.insert(70);
    s1.insert(10);

    multiset<int>::const_iterator it = s1.begin();
    for(;it!=s1.end();it++)
    {
        cout<<*it<<"";
    }
    cout<<endl;
    
}

int main(int argc, char *argv[])
{
    test06();
    return 0;
}


```

## 8.pair对组 










